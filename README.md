# ThreadPool only standart libraries realization.

# Создан ThreadPool который при вводе пользователем "restart" увеличивает количество исользуемых потоков, если это возможно.
# ThreadPool получает задачу по поиску всех простых делителей первого введенного пользователем числа, 
# второе введённое число указывает на приоритет задачи.

# Взаимодействие с пользователем реализовано через класс Menu в котором происходят основные вызовы.
# Класс Task реализует само вычисление задачи по поиску делителей.
#
# Все решение реализовано на чистом C++20 + STL без испoльзования Boost, Qt, TBB и сторонних библиотек.
#